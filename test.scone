module test;

#def main{T}(a, b: Vec2{T=T}[+(!ba)[].f(t=24) + 0x13], result: i32)(
#	#var c: i32;
#);

#def dot[T](a, b: array[3; Vec2[T=T]]) -> T {
#	#return a.x * b.x + a.y * b.y;
#};

def dot[T](a: Vec2[T], b: Vec2[T]) -> T {
	#return a.x * b.x + a.y * b.y;
};

struct Vec2[T] {
	x: T;
	y: T;
};
